
@{
    ViewBag.Title = "Login";
    Layout = "~/Views/Shared/_LayoutPage1.cshtml";
}



<!--<!DOCTYPE html>
<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Bank Login</title>-->
    <!-- Bootstrap CDN -->
    <!--<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
    <style>
        body {
            background: linear-gradient(to right, #4facfe, #00f2fe);
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        .login-container {
            margin-top: 100px;
        }

        .card {
            border-radius: 15px;
            box-shadow: 0px 0px 15px rgba(0,0,0,0.2);
        }

        .card-title {
            font-weight: bold;
            color: #333;
        }

        .btn-primary {
            background-color: #4facfe;
            border: none;
        }

            .btn-primary:hover {
                background-color: #007bff;
            }
    </style>
</head>
<body>
    <div class="container login-container">
        <div class="row justify-content-center">
            <div class="col-md-5">
                <div class="card p-4">
                    <h3 class="text-center card-title mb-4">Bank Login</h3>
                    @using (Html.BeginForm("Login", "Account", FormMethod.Post))
                    {
                        <div class="form-group mb-3">
                            <label for="Username">Username</label>
                            @Html.TextBox("Username", null, new { @class = "form-control", placeholder = "Enter username", required = "required" })
                        </div>
                        <div class="form-group mb-3">
                            <label for="Pwd">Password</label>
                            @Html.Password("Pwd", null, new { @class = "form-control", placeholder = "Enter password", required = "required" })
                        </div>
                        <div class="form-group mb-3">
                            <button type="submit" class="btn btn-primary w-100">Login</button>
                        </div>
                        <p class="text-danger text-center">@ViewBag.ErrorMessage</p>
                    }
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>-->

<div class="container mt-5" style="max-width: 400px;">
    <h2 class="text-center mb-4">Login</h2>

    @if (ViewBag.Message != null)
    {
        <div class="alert alert-danger">@ViewBag.Message</div>
    }

    @using (Html.BeginForm("Login", "Bank", FormMethod.Post))
    {
        <div class="form-group mb-3">
            @Html.DropDownList("Role", new List<SelectListItem>
            {
                new SelectListItem { Text = "Customer", Value = "Customer" },
                new SelectListItem { Text = "Employee", Value = "Employee" },
                new SelectListItem { Text = "Manager", Value = "Manager" }
            }, "Select Role", new { @class = "form-control" })
        </div>

        <div class="form-group mb-3">
            @Html.TextBox("Username", null, new { @class = "form-control", placeholder = "Username" })
        </div>

        <div class="form-group mb-3">
            @Html.Password("Password", null, new { @class = "form-control", placeholder = "Password" })
        </div>

        <button type="submit" class="btn btn-primary w-100">Login</button>

        <div class="mt-3 text-center">
            <a href="@Url.Action("Register", "Bank")">New Customer? Register here</a>
        </div>
    }
</div>